@PACKAGE_INIT@

if(NOT TARGET volumeshOS::volumeshOS)
    include("${CMAKE_CURRENT_LIST_DIR}/volumeshOSTargets.cmake")
endif()

set(volumeshOS_LIBRARY volumeshOS)
set(volumeshOS_LIBRARIES volumeshOS)
set(volumeshOS_LIBRARY_DIRS "@PACKAGE_CONF_LIB_DIRS@")
set(volumeshOS_INCLUDE_DIRS "@PACKAGE_CONF_INCLUDE_DIRS@")
set(volumeshOS_FOUND TRUE)
